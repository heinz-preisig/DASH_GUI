# Schema App v2 - TODO & Status
# Last updated: May 8, 2026

## Completed
- [x] Multi-tenant backend (abstract_events, session_manager, multi_tenant_backend)
- [x] All Qt dialogs backed by .ui files
- [x] UIStateManager + SchemaState automaton
- [x] schema_core.py: UIMetadata, groups, parent-child tree, schema_refs (sh:node embedding)
- [x] shacl_export.py: flat + hierarchical Turtle export; auto-save .ttl on every Save
- [x] dash_integration.py: DASHFormGenerator → config dict + standalone HTML form
- [x] SchemaGUI mixin refactor (May 2026):
      schema_gui.py reduced to ~250 lines (wiring only)
      - mixins/schema_management.py  — CRUD, export, validate, extend, daisy-chain, library, preview
      - mixins/component_list.py     — flat list, add/remove brick, add schema ref, context menu
      - mixins/component_tree.py     — tree rendering, groups, parent-child, schema-ref expansion
      - mixins/brick_panel.py        — brick library, search, details
      - mixins/flow_management.py    — flow type, flow editor dialog, flow steps list
- [x] Component tree view with list/tree toggle (componentViewStack)
      Root ◆ node, ▼ group branches, recursive parent-child, ⬡ schema refs (read-only, grey)
      Context menus: set parent, move to group, remove, move up/down
- [x] Schema references: add (pick schema → pick attach brick → enter property path),
      shown in both list (blue ⬡) and tree (grey italic ⬡), removable via menu/button
- [x] PyQt6 migration (ItemDataRole, DialogCode, etc.)
- [x] test_tree_structure.py — tree validation, navigation, SHACL export, DASH form generation

## TODO — High Priority
- [ ] Drag-drop in tree view: drag brick onto group/parent without context menu
      (QTreeWidget dropEvent override, update schema_core parent/group on drop)
- [ ] Schema ref inline editing: allow changing property_path after creation
- [ ] DASH form dynamic repeated elements: sh:maxCount > 1 → JS add/remove row
- [ ] DASH nested schema ref sections as collapsible sub-forms in HTML output
- [ ] Web interface parity: Flask API endpoints for schema_refs + tree structure

## TODO — Medium Priority
- [ ] Pytest suite: schema_core.py tree/ref methods + headless Qt fixture for mixins
- [ ] Error handling: missing referenced schema → graceful tree placeholder (not crash)
- [ ] validate_schema(): check all component brick_ids actually exist in library
- [ ] BrickIntegration: cache get_available_bricks() to avoid re-reading disk each call
- [ ] Lazy tree expansion for schemas with > 50 components

## TODO — Low Priority
- [ ] Schema templates (pre-filled root + suggested components)
- [ ] Import from JSON Schema / XML Schema
- [ ] Version history / undo for schemas
- [ ] Collaboration / multi-user editing

## Run
  python main.py                              # Qt interface
  python test_tree_structure.py              # tree + DASH tests
  python test_shacl_export.py
  python test_ui_metadata.py
